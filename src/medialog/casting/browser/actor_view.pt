<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en"
    xmlns:tal="http://xml.zope.org/namespaces/tal"
    xmlns:metal="http://xml.zope.org/namespaces/metal"
    xmlns:i18n="http://xml.zope.org/namespaces/i18n"
    lang="en"
    metal:use-macro="context/@@main_template/macros/master"
    i18n:domain="medialog.casting">
<body>

<metal:content-core fill-slot="main">
<metal:block define-macro="content-core">

    <h1><span>${context/first_name|None}</span> <b>${context/last_name|None}</b></h1>
    <p>Født: <span class="pat-moment"
       data-pat-moment="format:calendar">
        ${python: context.born}
      </span>
    </p>

    <div class="flex">
      <figure tal:define="scale_func context/@@images;
             scaled_image python:scale_func.scale('image', scale='preview')">
        <img tal:replace="structure python:scaled_image.tag()"
             tal:on-error=""/>
      </figure>
      <div class="textblock">
      <p><span>Alder:</span> <span class="actor_value">${context/age|None} år</span></p>
      <p><span>Kjønn:</span> <span class="actor_value">${context/sex|None}</span></p>
      <p><span><!--i class="fa fas fa-address-card"/-->Kontaktinfo: <span class="actor_value">
        <i class="fa fa-home" tal:condition="context/adresse|None"/>  ${context/adresse|None} &nbsp;
        <i class="fa fa-envelope" tal:condition="context/email|None"/>  ${context/email|None} &nbsp;
        <i class="fa fa-phone" tal:condition="context/phone|None"/>  ${context/phone|None}</span>
      </p>
      <p><span>Etnisitet:   </span>
         <span class="actor_value">
           <tal:repeat tal:repeat='rep context/etnisitet|None'>
            <span>${rep}. &nbsp;</span>
          </tal:repeat>
         </span>
       </p>
      <p><span i18n:translate="">Språk:       </span>
      <span class="actor_value">
        <tal:repeat tal:repeat='rep context/language|None'>
          <span>${rep}. &nbsp;</span>
         </tal:repeat>
        </span>
      </p>
      <p><span>Dialekt:     <span class="actor_value">${context/dialekt|None}</span></p>
      <p><span>Øyenfarge:   </span><span class="actor_value">${context/eye_color|None}</span></p>
      <p><span>Hårfarge:    </span><span class="actor_value">${context/hair_color|None}</span></p>
      <p><span>Høyde:       </span><span class="actor_value">${context/height|None} cm</span></p>
      <p><span>Vekt:        </span><span class="actor_value">${context/weight|None} kg</span></p>
    </div>

    </div>
      <div class="infotext"><h4><i class="fa fa-info"></i> <b>Annen info</b></h4> <div tal:replace="structure context/text/output|None" />
    </div>

    <div class="annet">
      <p>
        <span>Førerkort: </span>
        <span class="actor_value" tal:repeat='klasse context/driving_licence|None'>
          <span i18n:translate="">• ${klasse}</span>
        </span>
      </p>
      <p>
        <span>Erfaring: </span>
        <span class="actor_value">
          <tal:repeat tal:repeat='rep context/erfaring|None'>
           <span>• ${rep}</span>
         </tal:repeat>
      </p>
      <p><span>Prosjekter: </span>
         <span class="actor_value">
            <tal:repeat tal:repeat="ref context/relatedItems|None">
                 <span>${ref/to_object/Title}. &nbsp;</span>
            </tal:repeat>
        </span>
       </p>
    </div>

<style>
  div.textblock {
	margin-right:  auto;
	margin-left: 3em;
}

div.textblock span.actor_value {
	font-weight: bold;
	margin-left: 0.5em;
}

#content div.flex {
	padding: 20px 0;
	margin: 16px 0 30px 0;
	border-top: 2px solid #4da5d9;
	border-bottom:   2px solid #4da5d9;
}

.infotext i {
	font-size: 1.4em;
	color:  white;
	xposition: relative;
	xposition: relative;
	background: #62a4d4;
	padding: 0.2em 0.5em;
  margin-bottom: 10px;
  margin-right: 0.5em;
	border-radius: 50%;
}
</style>

  </metal:block>
</metal:content-core>

</body>
</html>
