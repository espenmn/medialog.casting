<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en"
    xmlns:tal="http://xml.zope.org/namespaces/tal"
    xmlns:metal="http://xml.zope.org/namespaces/metal"
    xmlns:i18n="http://xml.zope.org/namespaces/i18n"
    lang="en"
    metal:use-macro="context/@@main_template/macros/master"
    i18n:domain="medialog.casting">
<body>

<metal:content-core fill-slot="main">
<metal:block define-macro="content-core">

    <h1><span>${context/first_name|None}</span> <b>${context/last_name|None}</b></h1>
    <p>Født: <span class="pat-moment"
       data-pat-moment="format:calendar">
        ${python: context.born}
      </span>
    </p>

    <div class="flex">
      <figure tal:define="scale_func context/@@images;
             scaled_image python:scale_func.scale('image', scale='preview')">
        <img tal:replace="structure python:scaled_image.tag()"
             tal:on-error=""/>
      </figure>
      <div class="textblock">
      <p><span>Alder:</span> <span class="actor_value">${view/get_age}</span></p>
      <p><span>Kjønn:</span> <span class="actor_value">${context/sex|None}</span></p>
      <p><span>Kontaktinfo: <span class="actor_value">${context/adresse|None}</span></p>
      <p><span>Etnisitet:   <span class="actor_value">${context/etnisitet|None}</span></p>
      <p><span>Språk:       <span class="actor_value">${context/language|None}</span></p>
      <p><span>Dialekt:     <span class="actor_value">${context/dialekt|None}</span></p>
      <p><span>Øyenfarge:   <span class="actor_value">${context/eye_color|None}</span></p>
      <p><span>Hårfarge:    <span class="actor_value">${context/hair_color|None}</span></p>
      <p><span>Høyde:       <span class="actor_value">${context/height|None} cm</span></p>
      <p><span>Vekt:        <span class="actor_value">${context/weight|None} kg</span></p>
      <p><span>Utdanning    <span class="actor_value">${context/education|None}</span></p>
    </div>
    </div>

    <div class="annet">
      <p>
        <span>Førerkort: </span>
        <span class="actor_value" tal:repeat='klasse context/driver_licence|None'>
          ${klasse}
        </span>
      </p>
      <p>
        <span>Erfaring: </span>
        <span class="actor_value">${context/erfaring|None}</span>
      </p>
    </div>

<style>
  div.textblock {
	margin-right:  auto;
	margin-left: 3em;
}

div.textblock span.actor_value {
	font-weight: bold;
	margin-left: 0.5em;
}

#content div.flex {
	padding: 20px 0;
	margin: 16px 0 30px 0;
	border-top: 2px solid #4da5d9;
	border-bottom:   2px solid #4da5d9;
}

</style>

  </metal:block>
</metal:content-core>

</body>
</html>
